---
platform: x
type: thread
angle: cognitive-framework
status: draft
date: 2026-03-14
source: content/website/final/structured-divergence.md
---

**1/**
went 6 weeks without ADHD medication without noticing.

Claude Code's file system was doing the job instead.

CLAUDE.md, handoffs, lessons.md, MEMORY.md - they hold the context your working memory can't. a thread on what I'm calling structured divergence:

**2/**
ADHD working memory holds 2-4 items before things drop. medication expands the buffer. that's all it does.

Claude Code's file system does the same thing structurally. context loads automatically. corrections persist as rules. knowledge survives across sessions.

**3/**
the convergence pattern is the interesting part. I keep watching builders go through 7 stages:

discovery --> trust unlock --> cost wall --> CLI pivot --> file system awakening --> system convergence --> builder identity

everyone arrives at stage 5 independently. same pain, same solution.

**4/**
stage 6 is wild. everyone builds the same outputs.

personal website. voice system. content pipeline. distribution automation.

the constraints converge so the builds converge. persistent context + self-improving rules = compounding systems.

**5/**
wrote the full thesis connecting it to the three open source repos:

recursive-drift (methodology)
context-handoff-engine (plumbing)
website-with-soul (product)

all MIT licensed. full post: [shawnos.ai link]
